// Module structure namespace for Notes domain.
public enum Notes {
    public enum Data {
        public enum Api {
            public enum DTO {}
        }
    }

    public enum Business {
        public enum Model {}
    }

    public enum UI {
        public enum Model {}
        public enum Component {}
        public enum Widget {}
        public enum List {}
        public enum Details {}
        public enum Create {}
        public enum Edit {}
    }
}
