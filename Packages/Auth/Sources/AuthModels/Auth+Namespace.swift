// module structure
public enum Auth {
    public enum Business {
        public enum Model {}
    }
    public enum UI {
        public enum Initial {}
        public enum LocalAuth {}
        public enum Model {}
    }
}
