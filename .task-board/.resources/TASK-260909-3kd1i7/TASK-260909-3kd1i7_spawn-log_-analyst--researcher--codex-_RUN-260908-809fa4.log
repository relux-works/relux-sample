OpenAI Codex v0.153.4
--------
workdir: /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
model: gpt-6-astra
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: low
reasoning summaries: none
session id: 01a082ec-0bd5-77b0-b891-f63a6aecd19e
--------
user
# Agent Task Assignment

## FIRST — Run Immediately

```bash
task-board m 'set_status(TASK-260909-3kd1i7, status=integrating)'
```

## Operational Constraints (Headless Run)

Read this before you run any gate or attach any outcome. A spawned run is
usually headless: the session ends when your turn ends.

- **Never background a long command and end your turn.** The session terminates
  with the turn and the process dies with it, while the run is still recorded as
  completed — a successful-looking run with no evidence attached.
- **A single shell call is time-bounded (~10 minutes).** Waiting out a longer
  check inside one call fails too. Split long verification into bounded
  sequential calls (package subsets, `-run` masks) and state explicitly what you
  reran yourself versus accepted from already-attached evidence.
- **Attach evidence before you end the lifecycle, not after.** "I will attach it
  when the run finishes" is unfulfillable by construction: once your turn ends
  there is no session left to attach anything.

## Your Role
# researcher

## Description

Researches questions, collects information, highlights key aspects, performs fact-checking of findings. Writes structured findings documents.

## Deliverable

`.research/{YYMMDD}_{topic}.md` or `artifacts/RESEARCH.md`
Outcome resources are named `<ELEMENT-ID>_<slug>.<ext>` — the completion guard matches on that
ID prefix, and a differently named artifact does not satisfy the handoff. Write the file outside the
managed worktree, then attach it with `task-board resource add`.

Final human-facing wording must say "ready for review" or "handed off to review", not "done", "complete", "finished", "final", or "готово", when the board status is `to-review`.

## Standing Orders

### Evidence Honesty Contract

1. Run each validation or gate command directly as a standalone process. Do not pipe it through `tee`; do not use a pipe chain unless `pipefail` is enabled and the gate command's real status is preserved.
2. Report the real exit code of every validation or gate command.
3. Report expected-red gates truthfully as failing: when a command is expected to fail (for example, `go test` in a package-less module), give its real non-zero exit code and a one-line expected-failure rationale; never present it as passing.
4. Check a checklist item tied to a command only after that exact command has actually run green with exit code 0. If it did not run or did not exit 0, leave the item unchecked.
5. For board reads, use compact task-specific projections. A concrete assignment does not need routine `summary()`, `plan()`, `schema()`, or `{ full }`; request scoped schema only after an unknown call.

## Status Transitions

- **start_status:** `analysis`
- **end_status:** `to-review` (review handoff, not accepted done)

## Constraints

None — full read/write access to research artifacts.

## Skills

These role skill references are a lazy catalog, not a mandate to bulk-read every
body. Before technical work, identify the skills relevant to this task's concrete
scope and read those full skill bodies. Always read any skill explicitly required
by the task, user, or project instructions.

- **project-management**: `/Users/iv/.claude/skills/project-management/SKILL.md`

## Definition of Done

- [ ] Verify exercises and setup documentation against final paths and commands
- [ ] Implementation matches AC
- [ ] Solution fits project architecture
- [ ] Tests green
- [ ] Gate, refusal, validation, authorization, and attestation behavior attacked, not read — positive-path-only evidence is not accepted
- [ ] If review does not accept the work — verdict evidence added and status routed by the explicit verdict branches
- [ ] Findings written to file
- [ ] Key aspects highlighted
- [ ] Fact-checking performed — claims verified, sources cited
- [ ] Findings linked on the board as a new task-scoped outcome resource
- [ ] All questions from task description answered
- [ ] Important findings, decisions, anomalies, or regressions recorded in logbook when relevant
## Your Task

- **ID**: TASK-260909-3kd1i7
- **Title**: TASK-260909-3kd1i7: write-learning-exercises
- **Parent**: STORY-260909-3g9457
### Description

Produce a concise English learning-exercise outline as a task-scoped board outcome only, not product repository edits. Inspect the sample and verified audit in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-q49ois/worktree read-only; UI task is currently active there, so do not edit or test it. Read authoritative swift-relux source/docs as needed. Create a progression of practical extensions teaching best patterns: user-visible goal, pattern, current source entry points, acceptance checks, pitfalls. Distinguish implemented behavior from proposed functionality. Include recommendations for concise documentation corrections. The later diagram/documentation task will publish the guide against final UI code. No new infrastructure, no exhaustive research, no deep-research skill. Outcome should be compact and directly reusable.
### Acceptance Criteria

Concise source-grounded exercise outline is attached to task; actual vs proposed functionality is explicit

## Instructions

The following instructions have been attached to this task:

### learning-guide-scope.md
> Learning outcomes

Provide a short progression of optional exercises grounded in final source paths. Start with a small note operation and reducer/flow tests; then persistence behind the service/fetcher protocol, cross-domain session cleanup/orchestration, and a headless CLI reusing business logic. For each state user-visible outcome, pattern to practice, a few entry points, acceptance checks and one common pitfall. Distinguish implemented demo functionality from proposed exercises. Do not force six packages on a tiny feature; discuss when HybridState is enough and when BusinessState + UIState is warranted. Explicitly avoid view dispatch, duplicated domain truth, and sagas containing provider details. All public docs English; README.ru remains a short synchronized pointer if maintaining a duplicate long manual adds drift.




## Board CLI Reference

You have access to `task-board` CLI for managing your work. All writes use the `m` (mutation DSL) subcommand:

```bash
# FIRST/LAST lifecycle commands:
# Use explicit status changes when the task flow requires it.
task-board m 'set_status(TASK-260909-3kd1i7, status=analysis)'       # analyst-style work
task-board m 'set_status(TASK-260909-3kd1i7, status=development)'    # implementation / testing work
task-board m 'set_status(TASK-260909-3kd1i7, status=reviewing)'      # reviewer handoff
task-board m 'set_status(TASK-260909-3kd1i7, status=blocked)'        # when blocked
task-board m 'set_status(TASK-260909-3kd1i7, status=to-review)'      # when your work is ready for review

# Track progress with checklist
task-board m 'check_item(TASK-260909-3kd1i7, item=1)'                        # check item N
task-board m 'add_checklist_item(TASK-260909-3kd1i7, text="Write tests")'    # add checklist item

# Add notes about your progress, decisions, blockers, or review findings
task-board m 'set_notes(TASK-260909-3kd1i7, text="your note here")'

# Save short text directly as outcome resources
task-board m 'add_resource(TASK-260909-3kd1i7, name=TASK-260909-3kd1i7_results.md, content="...", type=outcome, description="Description")'

# Attach an existing local file (screenshots, PDFs, logs, archives, research docs)
task-board resource add TASK-260909-3kd1i7 ./path/to/file --type outcome --name TASK-260909-3kd1i7_artifact.bin -d "Description"
```

## Spawn Run Control

Tracked background spawn runs expose `TASK_BOARD_RUN_ID` in the child environment.
If your work is long-running, check for operator directives at safe checkpoints:

```bash
task-board spawn status "$TASK_BOARD_RUN_ID"
task-board spawn directives "$TASK_BOARD_RUN_ID"
```

Current runtimes do not support direct inbound push into your active session.
Treat directives as cooperative checkpoint signals:
- persist your current notes/artifacts before acting on `cancel`-style requests
- only honor pause/reroute intent at a safe checkpoint
- if no directive is present, continue normally

## IMPORTANT: Saving Results

When you produce work products (research documents, design docs, screenshots, logs, archives, implementation notes), you MUST save them as outcome resources with names that include the task ID:

```bash
task-board m 'add_resource(TASK-260909-3kd1i7, name=TASK-260909-3kd1i7_results.md, content="...", type=outcome, description="Description")'
task-board resource add TASK-260909-3kd1i7 ./path/to/file --type outcome --name TASK-260909-3kd1i7_artifact.bin -d "Description"
```

If you revise the same artifact later, use `task-board m 'update_resource(...)'` or `task-board resource update ...` instead of creating a silent overwrite.

If you discover important findings, decisions, anomalies, regressions, or non-obvious constraints while working, record them in `logbook` as well as on the board.

This ensures your results persist on the board and are accessible to other agents and the coordinator. Spawn completion is expected to produce at least one new task-scoped outcome artifact before the task can cleanly remain in `to-review`.

## Evidence That Counts

A passing suite means nothing unless something in it would have failed.

- Any behavior that GATES, REFUSES, VALIDATES, AUTHORIZES, or ATTESTS ships with negative tests that fail when the gate admits what it must reject. A positive test proves the gate is reachable, not that it works.
- Prove a bound by NARROWING the gate, not only by deleting it. A delete-only mutant proves the gate exists and says nothing about the class it covers.
- Prove behavior by driving the real entry point — launch, materialize, resolve, publish — and name the production call site. A helper that is unit-tested but called from nowhere promises nothing.
- Standard negative shapes to test and to look for: forged or self-minted evidence; absent evidence treated as satisfied; the check present but uncalled from production; a bypass path around the check; a capability claim that does not reproduce.
- A gate reports coverage as a measured ratio, not as prose: prose cannot distinguish 7 of 7 from 3 of 7, and "nothing is there" is not "nothing this checker sees". Name the blind spot as a stated bound.
- An absence and a failure to read are different facts. A failed, partial, or malformed read is never a legitimate absence, and a fallback defined for absence must not fire on a read failure.
- Prove, or report nothing. Where a property cannot be established, report unknown instead of inferring it from a proxy signal; callers act on a plausible guess.

Shapes vocabulary and the incident record behind each rule: `references/negative-evidence.md` in the project-management skill.

## Stop-The-Line: No Forced Fits

Do not keep implementing when autonomous work starts requiring a forced fit. A forced fit is any path where the task conflicts with a platform/API constraint, product decision, UX state model, ownership boundary, or architecture, and the remaining "solution" is mostly compensating hacks.

Warning signs:
- each fix needs another flag, stub, priority rule, mock-only behavior, or special-case test
- the tests can pass only because the test harness avoids the real platform behavior
- the implementation depends on an assumption you can no longer defend
- the user-facing behavior cannot be described cleanly without contradicting the product model

When this happens, stop product-code changes before adding another workaround layer. Attach or note:
- the constraint and evidence
- the failed assumptions/attempts
- the viable options and tradeoffs
- the recommended option
- the exact human/product/architecture decision needed

Then set the board item to `blocked` and ask only for that exact decision or external input. This stop applies only to a concrete external blocker or an unresolved human-only platform/product/architecture/tradeoff/approval decision; recoverable failures and ordinary rework stay autonomous. Tests and stubs are not proof that a forced-fit design is correct; use them only after the state model and platform assumptions are valid.

## Completion Discipline

Keep working until the task reaches a terminal handoff for your role. If no objective blocker remains, do not stop while the board item is still parked in `analysis`, `development`, `testing`, or `reviewing`.

Before your final status change:
- satisfy the task acceptance criteria and relevant checklist items
- attach outcome evidence for the work you produced
- run the relevant verification commands when the task changes code, tests, docs, or config

Use `blocked` only for either a concrete external blocker you cannot resolve autonomously or an unresolved human-only platform/product/architecture/tradeoff/approval decision. Record the constraint, evidence, failed assumptions/attempts, viable alternatives and tradeoffs, recommendation, and exact human decision or external input needed. Recoverable failures and ordinary rework are not `blocked`.

Status language is literal:
- `to-review` means your role has handed work to review; it does not mean the board task is accepted or done.
- In your final response, say "ready for review" or "handed off to review" when the final board status is `to-review`.
- Do not say "done", "complete", "finished", "final", or "готово" as the overall task state unless the board status is actually `done`.

## LAST — Run For Role Handoff

When you have completed all role work and the task is ready for its role handoff, run this as your **final board command**:

```bash
task-board handoff TASK-260909-3kd1i7 --role researcher
```

## Working Directory
Board directory: `/Users/iv/Developer/IV/ioscrew-demo/relux-sample/.task-board`

Work in the project root. Do not modify board files directly — always use the `task-board` CLI.

## Story Workspace

You are running in an isolated Git worktree for STORY-260909-3g9457.

- Workspace path: `.temp/STORY-260909-3g9457/worktree` (this is your working directory)
- Branch: `task-board/story/STORY-260909-3g9457`, forked from `main`
- Authoritative board: `/Users/iv/Developer/IV/ioscrew-demo/relux-sample/.task-board` (already exported as `TASK_BOARD_DIR`)

Make every repository change here. Do not commit, switch, rebase, merge, or delete this branch, and do not run `task-board` against the `.task-board` copy inside this worktree — it is a checkout artifact, not the board. Integration into trunk is the orchestrator's step, not yours.

Leave your work UNCOMMITTED in the worktree — the handoff snapshots the working tree against the recorded checkpoint. A commit of your own on this branch is refused at handoff with `change_request_candidate_committed_past_checkpoint`, and the repair is `git reset --soft <checkpoint_oid>` before completing again.

## Integration Assignment

This is the integration run for accepted Change Request `CR-TASK-260909-3kd1i7-1` revision 1. The immutable producer binding is role `researcher` (archetype `analyst`). Keep the board at `integrating`; only the integration transaction may write `done`. This assignment supersedes generic producer FIRST/LAST status and handoff instructions: run the bound checkpoint or integration command, attach fresh task-scoped outcome evidence, then end without calling the generic `handoff` command or setting status yourself.

codex
I’ll inspect the accepted change request and integration instructions, then attach fresh evidence for the research outcome. I’m using the project-management skill for the board workflow.

exec
/bin/zsh -lc "task-board m 'set_status(TASK-260909-3kd1i7, status=integrating)'" in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 93ms:
{"ok":true,"result":{"primary":{"action":"status_changed","element_id":"TASK-260909-3kd1i7","field":"status","new_value":"integrating","old_value":"integrating"}}}

exec
/bin/zsh -lc 'cat /Users/iv/.claude/skills/project-management/SKILL.md' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 0ms:
---
name: project-management
description: >
  File-backed task boards and spawn-first multi-agent orchestration through
  task-board. Use for board/spec decomposition, task tracking, planning,
  status/progress, tracked producer/reviewer runs, and delivery goals. A normal
  primary session is the Orchestrator; a prompt-assigned role or
  TASK_BOARD_RUN_ID is a specialist worker. Russian triggers: борд, доска,
  задачи, эпик, стори, таски, спавн, оркестратор, субагент, статус, прогресс.
triggers:
  - task board, epic, story, task, bug, status, progress
  - spec decomposition, planning, critical path
  - spawn, orchestrator, coordinator, sub-agent, delivery goal
  - борд, доска, задачи, эпик, стори, таски, спавн, оркестратор
---

# Project Management Router

Load only the focused references needed. Use `task-board` for reads/writes;
never edit `.task-board/` directly.

## Active Role

- **Specialist:** when assigned a role, `TASK_BOARD_RUN_ID` exists, or the
  human explicitly requests inline execution. Follow lifecycle/evidence gates.
- **Orchestrator:** otherwise. Detail settled specs inline; route
  every artifact-producing or validating task through a tracked
  background child run. `solution-architect` only for genuine
  architecture decisions.

## Critical Invariants

1. Preflight with `project_config(view=spawn-preflight, role=ROLE, agent=RUNTIME?)`;
   bare `project_config()` stays the role-optional config surface.
   Cross-provider ranking requires the agent-less query: omit `agent`, compare
   recommendations, then narrow with `agent=RUNTIME` for the fresh spawn snapshot.
   `providers.targeted` names the query's runtime scope; policy-allowed entries
   outside it use `runtime_not_targeted_by_query`, while policy-excluded entries
   use `provider_not_allowed_by_preferred_agentic_system`.
   When workload recommendations are configured: query with `task_class` or an
   explicit `workload_class`, reason about scope/risk/autonomy/validation and
   context cost, then spawn an explicit pair with the exact class transport,
   fresh snapshot digest, and one-line recommendation rationale. A ranking is
   advice, never authority, a default, or permission to widen the pair.
   For a proprietary (paid) model, choose the pair that maximises delivered
   work per token and escalate only for a stated reason.
   Every child has explicit role, background mode, and a complete
   model/reasoning-effort pair across its applicable dimensions — model
   either typed or seeded by a configured per-role default, and, for Codex,
   Qwen, or a Claude model whose registry row declares a reasoning-effort
   vocabulary, an in-policy effort either typed or default-seeded the same
   way. Built-in runtimes: `agy`, `claude`, `codex`, `gemini`, `muse`, `qwen`
   (plus any runtime declared under `spawn.runtimes`); Gemini, Agy,
   `muse-spark-1.2-contributor`, and an effortless Claude model take no
   `--reasoning-effort` at all, while `muse-spark-1.3-contributor` and its
   `muse-spark` alias REQUIRE one from `high`/`xhigh`/`max`. The
   versioned-short `muse-spark-1.3` names no registered row and is not
   spawnable; codex `gpt-6-astra`/`astra` REQUIRE one of `low`/`medium`/`high`/`xhigh`/`max`/`ultra`.
   Treat the v3 `admitted_pairs` set the
   preflight reports as the exact admission authority, not `allowed_models`,
   which is one input form and is `null` when the runtime uses combinable
   `entries`; when preflight reports a v2 migration warning, use the
   full read-only migration snapshot for an explicit reviewed repository edit.
   Never infer admission from `PolicyRank` or silently migrate config.
   Codex-only `fast_mode` defaults false and forces direct/managed
   `service_tier` default/priority; preflight records root/remote provenance.
   Remote-v4 parity is owned by the client projection and its checked fixtures;
   authority schema v1 intentionally authorizes an empty per-board backend
   policy set, so do not infer or add a server policy route.
2. Workers use compact, task-specific projections. Skip routine
   `summary()`, `plan()`, `schema()`, and `{ full }`; scoped schema only
   repairs an unknown call.
3. Default to `task-board spawn observe RUN --until terminal --format compact`,
   then route its handoff. Other reads are for barriers, replay/follow,
   diagnosis.
4. The Orchestrator owns producer -> reviewer -> rework -> reviewer until the
   configured review policy is satisfied and the board reaches `done`.
   `to-review` is a routing trigger, not human acceptance. Commit
   decomposition is that same Orchestrator's own call, never a per-commit
   human checkpoint.
5. Handoff needs a task-scoped outcome and `task-board handoff TASK-ID --role
   ROLE`. Early exits and intermediate parked states are
   retried, or rerouted through a focused child; routine work is never handed to the human.
   Producer `set_status(to-review)`
   is refused, and reviewer `accept_cr` requires its live merged checklist.
   Attach artifacts through resource CRUD under
   `.task-board/.resources/<ELEMENT-ID>/`, optionally in safe nested paths;
   never write that tree directly or attach directories.
6. `blocked` needs an evidence-backed external blocker or unresolved
   human-only product/platform/architecture/approval decision. Persist
   constraint, evidence, failed assumptions/attempts, viable alternatives,
   tradeoffs, recommendation, and the exact decision or external input needed.
7. At `to-review` say **ready for review** / **handed off to review**;
   done/complete/final only for `done`.
8. `board-goal-v2` is authoritative: `goal` owns `parent/primary`; `spawn goal`
   owns `run/RUN-*`. V1 shapes are compatibility-only.
9. `openai-board` / `anthropic-board` launch the primary `task-board
   codex|claude` sessions.
   agents-infra composes; `tb-sessiond` owns goal bind, location-scoped
   `openai-board|anthropic-board resume [SES-*]`, native identity restart, and
   session operations. Provider-native adoption remains explicit as
   `codex resume THREAD_ID` (compatibility form) or `codex --resume THREAD_ID`,
   and `claude --resume UUID`.
   Bad records move to private `<state>/quarantine/`; listen continues and
   status/list report them.
10. Freeze stable project/config/runtime/control roots and one binding+digest.
    Explicit config must exist; direct/managed children override ambient roots.
    `TASK_BOARD_DIR` stores, not authority. Resolve authority from a
    binding via fresh `git ls-remote --symref <authorized-url> HEAD`,
    exact-ref fetch, and equal advertised/fetched OIDs. Never use cached/local
    refs, tracking, caller/config fields, or prior tuples. Cross-check
    `integration_base_branch` against the independent branch/remote
    pair. Zero/multiple/unavailable/indeterminate evidence refuses.
    Producer publishes a Change Request, reviewer runs `accept_cr`,
    then Orchestrator checkpoints leaves and integrates the Story. Managed
    commits use `-S` with repository human identity/key; index failures never
    justify broad reset. See
    [tracked background spawn](references/tracked-background-spawn.md#story-worktree-isolation).
11. Cross-Story `set_parent` and integration are durable and fail closed; move
    evidence never widens source-tombstone admission. See
    [tracked background spawn](references/tracked-background-spawn.md#what-the-board-state-commit-covers-and-what-it-leaves-alone).
12. Board validation stays strict by default. The project setting
    `validation.allow_legacy_empty_done_containers=true` suppresses only the
    historical empty-`done` Story/Epic aggregate mismatch; active or
    child-backed containers remain strict.
13. Transaction-owned `.activity/<ID>/events.ndjson` forbids caller logging.
    Reads are bounded; corrections privileged/append-only. Transfer/worktrees
    reject foreign history, preserve legacy/deletion boundaries, and never backfill.
    See [file format](references/file-formats.md#activity-streams),
    [query](references/querying.md), and
    [coverage](docs/activity-mutation-coverage.md).
14. Never run `make install*`, `scripts/setup.sh`, or `task-board self-update`
    from inside a manager-hosted session. A Claude host's PTY master lives in
    the `tb-sessiond` process, so replacing that daemon hangs up every hosted
    session including the caller's own. A binary swap is a human action taken
    between sessions; the manager restarts from an outside shell with
    `task-board session restart`. See
    [goals recovery](references/goals-recovery.md#1-a-skill-reinstall-beside-a-running-tb-sessiond).

## Decision Tree

```text
Prompt-assigned role or TASK_BOARD_RUN_ID? -> work as that specialist
Artifact, test, docs, research, or review? -> tracked role-based spawn
Producer reached to-review? -> spawn reviewer, route verdict
Concrete stop boundary? -> persist packet, then blocked
Known structured board operation? -> q/m DSL with narrow projection
Unknown name/field? -> schema(); known call syntax? -> schema(operation|mutation=NAME); see querying recovery examples
```

## Lazy Reference Routes

| Need | Read |
| --- | --- |
| Queries, observability, goals, estimates, deps | [querying](references/querying.md), [file formats](references/file-formats.md) |
| Status, review, recovery, Stop-The-Line | [statuses](references/statuses.md) |
| Resources, bugs, evidence | [resources and bugs](references/resources-and-bugs.md) |
| Detailing, plans, execution | [planning workflow](references/planning-workflow.md) |
| Research and synthesis | [research workflow](references/research-workflow.md) |
| Roles, task classes, DoD | [roles](references/roles.md) |
| Negative tests, gate proofs, unknown vs absence | [negative evidence](references/negative-evidence.md) |
| Spawned goals, selection, runs, recovery, Story worktrees | [tracked background spawn](references/tracked-background-spawn.md) |
| Change Request order, checkpoint, republish | [CR lifecycle](references/change-request-lifecycle.md) |
| Session Manager wrappers, goal runtimes | [codex goal runtime](docs/codex-goal-runtime.md), [claude goal runtime](docs/claude-goal-runtime.md) |
| Templates, reviewer routing | [sub-agent templates](references/sub-agent-templates.md) |
| Context budgets | [context efficiency](references/context-efficiency.md) |
| Config feature gates | [config features](references/config-features.md) |
| Goal daemon skew, native recovery | [goals recovery](references/goals-recovery.md) |
| Project config refusals/schema/examples, authority envelopes, guarded alias migration, spawn supervision in minutes | [canonical config reference](references/task-board-config.md); persist a matching scoped selection before removing legacy `remote.user`, preview with `task-board migrate-project-config`, then apply only with its reviewed `--expected-digest` |
| Install/bootstrap | [setup](references/setup.md) |
| Remote auth/admin/ops | [remote board runbook](docs/remote-board-operations.md) |
| CLI/deploy overview | [README](README.md) |

Runtime changes follow canonical `.specs/` contracts.

Built-in spawn runtimes: `agy`, `claude`, `codex`, `gemini`, `muse`, `qwen`
(plus any runtime declared under `spawn.runtimes`). Gemini CLI 0.54.4 is
deprecated in favour of Antigravity, but both remain supported.

```bash
task-board spawn TASK-ID --role developer --background --agent agy --model gemini-3.6-flash-high --timeout 35m
```

`agy` takes no `--reasoning-effort`; effort is encoded in its eight scoped
model IDs. Minimum `agy` is 1.1.12; 1.1.14 is validated. Prompt delivery has
a 96 KiB argv budget; `SUCCESS`/`ERROR` JSON overrides process exit. Only the
top-level worker is tracked. See [README](README.md) for the full models,
flags, JSON, safety, configuration, and Gemini compatibility contract.

exec
/bin/zsh -lc 'cat /Users/iv/.claude/skills/project-management/references/change-request-lifecycle.md' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 0ms:
# Change Request Lifecycle Runbook

Orchestrator-owned. An accepted Change Request is accepted work, not delivered
work. This page states the order, the recovery paths, and the republish
semantics that used to live only in an operator's head.

The full typed contract — how a candidate is snapshotted, what
`repository_delta` means, what a `stale` revision is, and every refusal you can
hit — stays in
[tracked background spawn](tracked-background-spawn.md), section
`Change Requests, review, rework, and checkpoint`. This page is the order of
operations on top of it.

---

## The Change Request lifecycle, in order

1. **Accept.** The reviewer runs
   `task-board m 'accept_cr(<ELEMENT-ID>, revision=<N>, evidence=<ELEMENT-ID>_review-verdict.md)'`.
   The element moves to `integrating`. It is accepted, not delivered. Only the
   reviewer run that was handed the revision may accept it.
2. **Checkpoint immediately.** For every non-final leaf, run
   `task-board worktree checkpoint <ELEMENT-ID>` as the next action after
   acceptance. The accepted tree lands on the Story branch only, and the leaf
   stays `integrating` until Story integration lands it on trunk. Delay here is
   what makes the next producer's Change Request go `stale`.
3. **Checkpoint refreshes the real index automatically.** Candidate capture
   records (but never mutates) the real-index tree. Under the bound run's Story
   lease and the candidate-captured index CAS, checkpoint replaces only that
   captured state and independently verifies branch/HEAD/index/worktree
   identity. A typed
   `change_request_checkpoint_index_unsynced` refusal is retried after its named
   lock, I/O, or ownership condition is resolved; do not use a broad manual
   reset that can erase unreviewed staged state.
4. **Final leaf: integrate, do not checkpoint.** `checkpoint` refuses the
   Story's last open leaf with `change_request_final_leaf_checkpoint`, because
   closing it would promote a Story whose branch has never reached trunk. Land
   the Story with
   `task-board worktree integrate <STORY-ID> --cr <ELEMENT-ID> --revision <N>`.
5. **When `integrate` refuses, report the refusal.** `validation_not_configured`
   (`BUG-260807-1o5092`, the as-shipped behavior) and
   `integration_indeterminate` for a board that lives outside the control root
   are blockers, not detours. Report the refusal as the blocker it is and land
   only by the documented manual signed substitute — an isolated clone, signed
   cherry-pick of the exact reviewed head, a platform review record, and a
   fail-closed plain push. Never route around a typed refusal by moving board
   files or the branch by hand.
6. **A lost run is recovered by republish, never by re-running the producer.** A
   run that died after producing content but before the board recorded it still
   has its content on disk. Republish it with provenance. Re-running the
   producer discards work that already exists and costs the run again.
7. **A republish of an unchanged accepted tree is an annotation, not a new
   revision.** A candidate whose `repository_delta` is byte-identical to the
   accepted one, and whose acceptance still stands, is not a new candidate.
   Recording it as a revision turns the revision count — the only rework
   measure the board has — into process noise.
8. **Never commit manually on the Story branch.** The managed worktree's tree is
   published by the producer's handoff and landed by `checkpoint` or
   `integrate`. A hand commit on that branch makes the accepted snapshot and the
   branch head disagree, and the disagreement surfaces later as an unexplained
   `stale` or an integration that lands the wrong tree. A `task_delta` handoff
   now fails closed on it: the tip is compared to the workspace record's
   `checkpoint_oid` before the validation suite runs and before anything is
   written, and a disagreement is refused with
   `change_request_candidate_committed_past_checkpoint`. The producer repairs it
   inside its own run with `git reset --soft <checkpoint_oid>` — the work stays
   in the worktree — and completes the handoff again.

## A revision that will never be integrated

Two shapes end a Change Request revision without a reviewer verdict, and they
are different facts.

### An empty delta does not block the Story

A revision whose `repository_delta` is `empty` — an analyst or research run that
changed no repository file — is created `ready` like any other, because
emptiness is an attribute of the candidate and never a state. What changed with
`BUG-260906-37s3ps` is what that revision does to everybody else: while it sits
`ready` or `reviewing`, it no longer refuses a sibling producer with
`change_request_sibling_producer_blocked`.

The gate's argument is contamination — the new producer writes into the same
Story worktree the pending candidate was snapshotted from — and an empty
candidate has no repository work product for those writes to reach. Its
checkpoint writes no commit, and its integration is a no-op. Before the fix a
research-only run deadlocked its whole Story: nothing was routed at a candidate
with nothing to review, and every later producer was refused with a blocker
nobody could clear.

The exemption is scoped, and the scope is deliberate:

| Blocking state | `repository_delta=present` | `repository_delta=empty` |
| --- | --- | --- |
| `ready`, `reviewing` | refuses siblings | **admits siblings** |
| `draft`, `changes_requested`, `stale`, `conflicted` | refuses siblings | refuses siblings |

The bottom row expects a REPUBLISH, and the next snapshot of the shared worktree
would pick up the sibling's writes. Emptiness today says nothing about the
revision that replaces it. A revision with no readable `repository_delta` —
legacy, or from a newer build — refuses: a value this build cannot read is not
evidence that the candidate is empty.

The revision is still fully auditable. It is not deleted, not hidden, and not
given a special state; it simply stops being a blocker.

### Retiring a revision: `withdraw_cr`

An empty delta is not the only revision that never reaches integration. A
candidate can be superseded, abandoned, or published by a run whose deliverable
turned out to belong somewhere else. Before `BUG-260906-37s3ps` there was no
route for that at all: `accept_cr` needs a reviewer verdict and a merged
checklist, `worktree abort` discards the entire workspace, and republishing only
produces another revision in the same unresolved state.

```bash
task-board m 'withdraw_cr(<ELEMENT-ID>, revision=<N>, reason="why this revision will never be integrated")'
```

It moves the revision to `aborted`, which is terminal on both predicates that
read the state: it stops refusing sibling producers, and it stops withholding
the Story workspace's base convergence. The revision file and its ledger row
stay readable.

Four things it refuses, and why:

- **A revision that is `accepted`, `checkpointing`, `checkpointed`,
  `integrating` or `integrated`** — `change_request_state_conflict`. Those
  carry a reviewer's attestation, or commits that are already on the Story
  branch or in trunk. Recording one as never integrated would make the board
  disagree with git history.
- **A missing or whitespace-only `reason`** —
  `change_request_withdraw_reason_missing`. The ledger row is the only surviving
  explanation of why the revision exists and was never integrated.
- **A call from inside a tracked spawn run** —
  `change_request_withdraw_unauthorized`. Withdrawal is an ORCHESTRATOR route.
  A producer refused by `change_request_sibling_producer_blocked` must not clear
  its own blocker, and a producer handed `changes_requested` must not retire the
  rework signal instead of answering it.
- **A revision number that is not the current one** —
  `change_request_revision_conflict`, the same CAS every other transition takes.

`withdraw_cr` deliberately does NOT move the board element. Where the element
goes next — back to development, `blocked`, or closed as a duplicate — is a
routing decision the orchestrator makes with information the mutation does not
have; make it explicitly, in a separate call.

## Two same-class findings mean a missing gate

When two consecutive reviews of one leaf raise the same finding class, stop
routing another rework revision. Spawn one task to add a gate for the class — a
conformance vector set, a checker, a mutant harness — and let the next revision
be reviewed against the gate rather than against a reviewer's memory. The
reviewer's `repeat-of:` verdict field is the signal to route on.

## Recovery surfaces

- Stopped or refused integration: `task-board worktree transaction show <STORY-ID>`,
  then the disposition surface it names.
- A leaf whose handoff guard refused for artifact naming: attach the correctly
  named `<ELEMENT-ID>_<slug>.<ext>` outcome resource and re-run the handoff. Do
  not respawn the producer to satisfy a naming rule.

exec
/bin/zsh -lc 'task-board --help' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 0ms:
Manage a file-based task board with epics, stories, tasks, and bugs.

Usage:
  task-board [command]

Available Commands:
  admin                  Server administration (users, tokens, access)
  boards                 List boards accessible to the current user (remote mode)
  claude                 Launch Claude through the Task-board Session Manager
  codex                  Attach Codex to the Task-board Session Manager
  completion             Generate the autocompletion script for the specified shell
  export                 Export current board into a tar.gz archive
  goal                   Read and align the board-scoped parent primary goal
  grep                   Full-text search across board files
  handoff                Verify role handoff evidence and move to the role end status
  help                   Help about any command
  import                 Import a tar.gz archive into the current board
  limits                 Inspect and control provider rate-limit state
  login                  Store a token for the current remote
  logout                 Remove active user's token for current remote
  m                      Execute a mutation (write operation)
  migrate-keychain       Import legacy remote credentials into macOS Keychain
  migrate-project-config Preview or apply guarded deprecated project-config alias migration
  plan                   Show execution plan with phases and critical path
  q                      Query the board with a mini DSL
  repair-links           List or remove unsupported container dependency links
  resource               Upload, update, download, and delete board resources
  self-update            Update task-board to the latest version
  session                Inspect and stop Task-board Session Manager sessions
  spawn                  Queue an agent spawn run for a board task
  switch-user            Switch active user for current remote
  tree                   Show board as hierarchical tree
  tui                    Launch interactive TUI
  validate               Validate board structure
  whoami                 Show current remote and active user
  worktree               Inspect and repair managed Story workspaces

Flags:
      --board-dir string         Path to the board directory (overrides TASK_BOARD_DIR and config local.board_dir) (default ".task-board")
      --context-profile string   Agent context profile override: full or lite
  -h, --help                     help for task-board
      --insecure                 Skip TLS certificate verification (for self-signed certs)
      --json                     Output in JSON format
      --no-update-check          Disable automatic update check
      --remote string            Remote board server URL (overrides TASK_BOARD_REMOTE)
      --remote-board string      Remote board ID (overrides TASK_BOARD_ID)
  -v, --version                  version for task-board

Use "task-board [command] --help" for more information about a command.

exec
/bin/zsh -lc 'task-board worktree checkpoint --help' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 0ms:
Checkpoint an accepted Change Request revision.

The accepted candidate tree becomes one internal commit on the Story branch,
created under a compare-and-swap on the branch tip. The leaf remains integrating:
nothing is landed on trunk, and internal checkpoints only delimit later Task
diffs and make the work recoverable before the final Story squash.

A Change Request with repository_delta=empty creates no commit — its tree would
equal its parent's — and the leaf likewise remains integrating.

Only the tracked integration run bound to the accepted revision's producer role
and archetype may run this command. Reviewers, orchestrators, and other producer
archetypes are refused.

Usage:
  task-board worktree checkpoint <ELEMENT-ID> [flags]

Flags:
  -h, --help   help for checkpoint

Global Flags:
      --board-dir string         Path to the board directory (overrides TASK_BOARD_DIR and config local.board_dir) (default ".task-board")
      --context-profile string   Agent context profile override: full or lite
      --insecure                 Skip TLS certificate verification (for self-signed certs)
      --json                     Output in JSON format
      --no-update-check          Disable automatic update check
      --remote string            Remote board server URL (overrides TASK_BOARD_REMOTE)
      --remote-board string      Remote board ID (overrides TASK_BOARD_ID)

exec
/bin/zsh -lc 'cat /Users/iv/.claude/skills/project-management/references/querying.md' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 0ms:
# Querying & Mutating the Board

Three primary access modes following the [agent-facing-api](https://github.com/relux-works/skill-agent-facing-api) pattern, plus CLI for humans:

## Concrete Assignment Default

When a prompt already supplies a Task/Bug ID and task inputs, query only fields
needed for the next decision:

```bash
task-board q --format compact 'get(TASK-260211-abc) { status checklist outcomeResources }'
task-board q --format compact 'get(TASK-260211-abc) { status blockedBy derivedBlockedBy }'
```

Do not add routine `summary()`, `plan()`, `schema()`, or `{ full }` orientation.
Use `schema(operation=NAME)` or `schema(mutation=NAME)` only after an unknown
call. Board-wide observability and plans belong to an orchestrator routing
decision; full reads remain explicit audit/debug surfaces.

## DSL Queries — primary read approach for agents

Compact queries that return structured output. Zero session overhead (no MCP handshake), batch multiple queries in a single call. **This is what agents should use for all structured reads.**

**Output format:** Use `--format compact` for token-efficient output (~40% fewer tokens). Default is `--format json`.

```bash
# Board overview
task-board q --format compact 'summary()'

# Whole-project readiness with the existing summary folded in
task-board q --format compact 'summary() { observability-project }'

# Scoped readiness at each board level
task-board q --format compact 'list(type=epic) { observability-epic }'
task-board q --format compact 'get(STORY-260211-def) { observability-story }'
task-board q --format compact 'list(type=task, status=development) { observability-task }'

# Single element lookup
task-board q --format compact 'get(TASK-260211-abc) { status assignee }'

# Bounded activity history (continue with cursor=<next_cursor>)
task-board q --format compact \
  'activity(TASK-260211-abc, limit=25, order=descending, kinds="progress,content", view=effective)'

# Filtered listing with field preset
task-board q --format compact 'list(type=task, status=development) { overview }'

# Agent monitoring
task-board q --format compact 'agents(stale=60)'

# Batch: multiple queries in one call (semicolon-separated)
task-board q --format compact 'get(T1) { status }; get(T2) { status }; get(T3) { status }'

# Zero-argument and parameterized operations can share a batch
task-board q --format compact 'project_config(); models(agent=codex)'

# Execution plan
task-board q --format compact 'plan(EPIC-01, mode=children)'

# Schema introspection — discover available fields, presets, operations, and mutations
task-board q 'schema()'
task-board q 'schema(operation=project_config)'
task-board q 'schema(mutation=set_status)'

# Target-only spawn policy (explicit opt-in in every profile)
task-board q --format compact 'project_config(view=spawn-preflight, role=developer, agent=codex)'
```

**Operations:** `get(ID)`, `activity(ID, cursor?, limit?, order?, kinds?, view?)`, `list(filters...)`, `count(filters...)`, `summary()`, `plan(scope?, mode=?, active=?)`, `agents(stale=N?)`, `models(agent?)`, `project_config(view?, role?, agent?)`, `whoami()`, `schema(operation?|mutation?)`
**Field projection:** `{ id status assignee }`; base presets: `minimal`, `default`, `overview`, `full`, `progress`, `planning`; observability presets: `observability-project`, `observability-epic`, `observability-story`, `observability-task`
**Batching:** semicolons between queries. Statements and results preserve source order. JSON batches return an ordered top-level array; compact batches format each result normally and separate adjacent results with one blank line. A single query remains unwrapped. Syntax and unknown-operation errors reject the whole batch before execution instead of returning partial output; handler errors remain ordered in-band error results and later statements continue. Element mutations in one batch run inside one activity transaction: when any statement is refused, the refused statements keep their own errors, every statement that would otherwise have succeeded is reported as `ok:false` with code `batch_rolled_back`, and nothing is persisted. A clean batch commits every statement and records one activity event per affected element, so a batch may change an element's checklist only once; issue further checklist changes as separate commands. Local and remote mutation commands both print that complete envelope before returning a nonzero exit, so the envelope always describes exactly what was persisted.
**Format:** `--format compact` (agent default) or `--format json` (TUI/programmatic)

**First-time setup:** Under the full profile, `schema()` returns the complete API contract. Under lite, skip the proactive full read and request only the failed/unknown call with `schema(operation=NAME)` or `schema(mutation=NAME)`. Parser errors for a known call include that same typed signature directly.

`activity()` is deliberately separate from element projections: ordinary
`get`/`list` reads never embed history. Its `limit` defaults to 50 and is bounded
to 1..100. `order` accepts `ascending` or `descending`, `view` accepts `raw` or
`effective`, and `kinds` accepts a comma-separated operation filter. Reuse only
the opaque `next_cursor` returned by the previous page and keep the same order;
the cursor is direction-bound and sequence-anchored for stable traversal across
concurrent appends. The local CLI and remote `/q` endpoint share this contract.
Activity is automatic infrastructure output; callers must not append routine
history themselves. An explicit `legacy_pre_feature` boundary means older
events are unavailable, not that they should be inferred. Deletion retains the
terminal typed history. Operators correct a bad record only with confirmed,
append-only `correct_activity`; the original remains in `view=raw`, while
`view=effective` applies the correction, and remote correction requires Admin.
Never replace bounded pages with `get(...){full}` or an unbounded filesystem
read.

`kinds` also selects the three DERIVED control-plane planes. `run` returns the
spawn run lifecycle mirrored onto the run's task, `change_request` returns the
Change Request transitions mirrored onto the element the CR is attached to, and
`workspace` returns the Story workspace operations mirrored onto the Story:

```bash
task-board q --format compact 'activity(TASK-260211-abc, kinds="run,change_request", limit=25)'
task-board q --format compact 'activity(STORY-260211-abc, kinds=workspace, limit=25)'
```

These events carry one typed `ledger_event` change — domain, ledger id,
enumerated event token, sequence, revision, from/to state tokens, a UTC
instant, and typed `oid`/`ref`/`id`/`token` facts. They never carry a summary,
message or reason: each ledger keeps its own human-readable text and the mirror
cannot transport it. The default unfiltered view includes them alongside board
mutations, and filtering by a board kind such as `kinds=progress` returns
exactly what it returned before the planes existed.

The runtime emits them automatically at the moment each ledger row is written.
`record_ledger_event` exists so the local and remote transports share one
statement and one validator, and it refuses any free-text payload.

**It is not an agent-facing mutation.** `task-board m 'record_ledger_event(...)'`
is refused, on a local and a remote board alike, with a
`ledger_mirror_unauthorized` error: a mirror is DERIVED from a ledger row, so
one appended by hand asserts a control-plane fact that never happened. On a
local board the refusal is enforced twice over — once on the CLI mutation
surface and once in the shared activity layer, which admits only the runtime's
derived-mirror sink as the source entrypoint.

**Stated bound — board-server boards.** The prevention above is a property of
the CLI, not of the server. A mirror bound for a remote board is relayed over
the same `m` transport, authenticating as the same principal, through the same
handler entrypoint, as any other mutation that principal could send, so the
server cannot tell a relayed mirror from one a write-capable principal composed
by hand — and it admits both. What the remote plane holds instead is
attribution: every mirror records the authenticated principal that sent it, so
a hand-minted one is traceable to its author even though it is not prevented.
Preventing it would need per-record attestation the transport has no field for;
admin-gating the operation would break the ordinary agent's mirror, which is
the behaviour the remote plane exists to provide.

**Server capability is proven, never assumed.** Before sending a mirror to a
board-server the CLI reads `/v1/version` and requires the `ledger_mirror`
contract. A server that advertises none, or a different version, is refused
with a message naming what it advertised — a server that cannot persist a
mirror would refuse `record_ledger_event` as an unknown operation, which looks
exactly like a mirror that was never attempted. A `/version` read that FAILS is
refused too, and separately: a failed read is not an absence, and treating it
as one would silently drop mirrors a capable server would have accepted.

Run heartbeats are deliberately not mirrored — see
`docs/activity-mutation-coverage.md` for that bound and for what
`task-board validate` reports when a ledger row has no mirror.

### Scoped signatures

`schema(operation=NAME)` and `schema(mutation=NAME)` return one flat signature object: `kind`, `name`, a typed `signature`, description, parameters, and examples; mutations also report `destructive` and `idempotent`. They do not include unrelated fields, presets, operations, or mutation contracts. Provide exactly one scope. `schema()` without a scope retains the full legacy response.

Known-operation parser failures preserve the underlying typed parse error and append the relevant signature. This lets an agent repair a call without reading the full schema.

### Recovery examples from the Story binary

Captured at Story commit `13afc672f01a38d5748cca533f33524aec8080d9`.
From the repository root, build without installing:

```bash
(cd tools/board-cli && go build -o ../../.temp/TASK-260907-bg79el/task-board .)
```

The commands below use that binary and the authoritative board selected by
`TASK_BOARD_DIR`. IDs refer to this Story's board; substitute existing IDs on
another board. Output blocks quote the exact emitted text (errors are stderr).
Exit 1 is an expected refusal, not a passing command.

#### Unknown operation

An unknown operation lists the known query operations and points to `schema()` for discovery and `schema(operation=NAME)` for one signature.

```bash
.temp/TASK-260907-bg79el/task-board q 'fetch()'
```

Output (exit 1):

```text
query error: parse error at 1:1: unknown operation "fetch" (got "fetch"); known operations: activity, agents, context, context_boundary, context_candidates, context_retention, contexts, count, get, list, models, plan, project_config, provider_limits, schema, summary, whoami; see schema() for the contract and schema(operation=NAME) for one signature
```

#### Statement grammar and bare positional arguments

The scoped signature includes `syntax`: the first bare positional value is the element/identifier argument, and subsequent arguments use `key=value`. Thus `get(TASK-260907-bg79el)` matches `get(id:string)`.

```bash
.temp/TASK-260907-bg79el/task-board q 'schema(operation=get)'
```

Output (exit 0):

```text
{"description":"Get a single element by ID","examples":["get(TASK-XX) { overview }"],"kind":"operation","name":"get","parameters":[{"name":"id","type":"string","optional":false,"description":"Element ID"}],"signature":"get(id:string)","syntax":"OPERATION(element-or-identifier, key=value, key=\"quoted string\") { field preset }; statements separated by ';'; the first bare positional value is the element/identifier argument; remaining arguments use key=value (no operators or lists); string escapes: \\\" \\\\ \\n \\t"}
```

#### Unknown projection field

A misspelled projection field suggests `updated` and points to `schema()` for valid fields and presets.

```bash
.temp/TASK-260907-bg79el/task-board q 'get(TASK-260907-bg79el) { updated_at }'
```

Output (exit 1):

```text
query error: parse error at 1:29: unknown field "updated_at" (got "updated_at"); did you mean `updated`?; see schema() for valid fields and preset names
```

#### Bare element ID

An element ID alone is refused; wrap it in `get(ID) { fields }`.

```bash
.temp/TASK-260907-bg79el/task-board q 'TASK-260907-bg79el'
```

Output (exit 1):

```text
query error: an element ID is not an operation; use get(ID) { fields }
```

#### Keyword IDs on mutations

Every id-first mutation accepts `id=...` (bare or quoted) as well as the first positional argument. This includes `link`, `add_checklist_item`, and `set_status`; supplying both forms is rejected. These successful dry runs preview without persisting changes.

```bash
.temp/TASK-260907-bg79el/task-board m --dry-run 'link(id=TASK-260907-bg79el, blocked_by=TASK-260211-1saw6h)'
```

Output (exit 0):

```text
{"ok":true,"result":{"blocked_by":"TASK-260211-1saw6h","dry_run":true,"id":"TASK-260907-bg79el"}}
```

```bash
.temp/TASK-260907-bg79el/task-board m --dry-run 'add_checklist_item(id=TASK-260907-bg79el, text="Verify recovery guidance")'
```

Output (exit 0):

```text
{"ok":true,"result":{"dry_run":true,"id":"TASK-260907-bg79el","text":"Verify recovery guidance"}}
```

```bash
.temp/TASK-260907-bg79el/task-board m --dry-run 'set_status(id=TASK-260907-bg79el, status=development)'
```

Output (exit 0):

```text
{"ok":true,"result":{"dry_run":true,"id":"TASK-260907-bg79el","status":"development"}}
```

### `project_config()` runtime shape

`project_config()` is always resolved by the local CLI, including when normal
board reads and writes target a remote server. Its current top-level keys are
`configured_mode`, `effective_mode`, `board_source`,
`explicit_remote_override`, `project_root`, `config_path`, `config_exists`,
`local`, `remote`, `whoami`, `goals`, `spawn`, `codex`, and `version_control`.

The no-argument shape remains the role-optional general config surface in every
profile, including the full default. For a spawn decision, opt in explicitly:

```bash
task-board q --format compact 'project_config(view=spawn-preflight, role=developer, agent=codex)'
```

`role` is required. `agent` is optional and otherwise follows the same
preferred-provider/schedule/runtime-affinity resolver as `task-board spawn`.
The narrow result contains `enabled`, `max_parallel`, a small effective
agent-context profile/source header, the effective allowed and target providers,
the active schedule window, canonical role, merged role ceiling for the target
provider, provider selection mode, and launch-composition enabled/status. It
excludes local/remote auth, `whoami`, Codex session, nested agent-context policy,
project paths, all unrelated provider ceilings, and version-control fields.
Active schedule allow-sets and provider refusals use the same fail-closed
resolver as spawn.

For Codex, `resolved_role_ceiling` includes the resolved boolean `fast_mode`,
`fast_mode_source` (`default` or `configured`), and `provenance.fast_mode` with
the exact root or remote key. Durable spawn status and JSON run state carry
`service_tier`, `fast_mode`, `fast_mode_source`, and `fast_mode_config_key`;
provider-limit retries update them to describe the provider currently being
launched, while each `provider_attempts[]` entry preserves the tier evidence
carried by that historical launch.

The lite profile's resolved `queries.project_config=spawn-preflight` setting
guides agents toward the target-specific call; it does not rewrite a bare
`project_config()` invocation. Supplying a view or target argument opts into
the narrow projection, whose role remains mandatory.

The policy members relevant to spawning and status mutation are:

| Path | Shape |
|---|---|
| `spawn.preferred_agent` | Legacy configured provider string, or `null`. It is retained only as compatibility evidence. |
| `spawn.preferred_agentic_system` | Currently effective `{mode, allowed_providers, source, config_key}`, or `null`. During an active schedule window, `source` is `schedule`. |
| `spawn.schedule` | `{configured, timezone, config_key, windows, active_window, base_preferred_agentic_system}`. `timezone` is always `UTC`; every projected window also carries its normalized bounds, key, and provider-system projection. |
| `spawn.launch_composition` | `{enabled, config_key, contract, schema_version, degradation}` without invoking `agents-infra`. |
| `spawn.ceilings` | Always reports effective `contract_version`, `authority`, `configured`, and `config_path`. V3 providers expose normalized `allowed_models` or `entries`, nullable `resolution_model`, confirmation mode, effort direction, merged role-effective policy, and exact provenance. Every contract version and input form reports `admitted_pairs`, the canonical admitted `(model, effort)` set with its digest — the field to read when deciding what a policy admits, since `allowed_models` is `null` under the entries form. An entries policy also reports `entries_expanded`, `entries_expansion_pinned`, and any unreachable/redundant entry `warnings`. V2 exposes `snapshot_version`, `snapshot_allowed_models`, `migration_required`, stable actionable warnings, and exact read-only v3 `migration` objects. |
| `spawn.selection` | Required flags plus provider effort vocabularies and supported model criteria. |
| `spawn.execution` / `spawn.tracking` | Executable background mode and durable run-observation contract. |
| `goals` | Static compact `board-goal-v2` capability with required Session Manager native reflection and the one-release `primary-goal-v1` / `board-goal-v1` adapters. The legacy `goals.primary` descriptor remains nested alongside the v2 fields for the compatibility release. It never embeds the current objective and is omitted from `view=spawn-preflight`. |
| `version_control` | `{configured, confirm, desired_commit_time, enforced_types, owner_message, acknowledgement_parameter, acknowledgement_value, config_key}`. |

Local mode uses `spawn.*` keys. Remote mode uses `remote.spawn.*` when that
object is present and non-null; otherwise it falls back to the whole root
`spawn` object. The reported `spawn.config_key_prefix` and nested `config_key`
values always identify the effective source. `preferred_agentic_system` is the
canonical config spelling; `preferred_agent_system` is not an alias.

`spawn-policy-v3` admission is exact membership in the effective
`allowed_models` set. `PolicyRank`, lifecycle, recommendation, registry order,
and price are not policy inputs. A v2 projection is compatibility evidence,
not an automatic migration: setup, query, spawn, and install never rewrite the
repository. For an explicit migration, copy the full projection's provider and
role `migration` objects under `contract_version: spawn-policy-v3`, review the
diff, then repeat full and target-role preflight queries. An effort-only v2
warning uses reason `spawn_policy_v2_effort_only_migration_required` and
explicitly says to omit `resolution_model` unless a fallback is intended.

### Project observability preset family

`summary() { observability-project }` returns the whole-project view. The
element presets work with the existing `get` and `list` operations; the task
preset also applies to Bugs because Tasks and Bugs share the leaf estimate
contract.

Every preset exposes the same nested `observability` field:

- `scope` identifies the project or selected Epic/Story/Task/Bug.
- `summary` carries the existing `byType`, `active`, and `blocked` summary,
  scoped to that element and its descendants.
- `readiness.estimateKind` carries the leaf or calculated
  Story/Epic/project projection state without dumping every missing leaf.
- `readiness.coverage` always reports `estimated`, `unestimated`, `total`,
  `ratio`, and `complete`, including fully estimated scopes.
- `readiness.rolledEstimate` is the known Fibonacci sum even when coverage is
  partial. Query the separate `estimate` field on the incomplete Story/Epic to
  retrieve its exact `unestimatedTaskIds` drill-down list.
- `isBlocked` / `blockedElementIds` aggregate hard dependency state across the
  scope, while `isAdvisoryBlocked` / `advisoryBlockedElementIds` aggregate
  derived cross-container waiting. The nested summary retains blocker details.
- Only the project preset adds `primary_goal`. Its active form is
  `{active:true,id,revision,objective,updated_at}`; after a clear or before
  any creation it is `{active:false,latest_ref}` (where `latest_ref` can be
  `null`). Epic, Story, Task, and Bug observability deliberately do not repeat
  this board-wide parent intent or treat it as element acceptance.

Plain `summary()` keeps its legacy response shape for compatibility.

## DSL Mutations — structured writes for agents

Write operations through the same DSL engine. All mutations support `--dry-run` preview. Destructive mutations require `--confirm`.

Element-ID mutations accept the first positional argument or `id=ID` (bare or quoted), matching their schema signatures: `link(ID, blocked_by=OTHER)` and `link(id="ID", blocked_by=OTHER)` are equivalent. Supplying both forms is rejected. `create(type=..., parent=...)` retains its keyword syntax.

```bash
# Status changes
task-board m 'set_status(TASK-12, status=development)'

# Evidence-backed role handoff (preferred producer end transition)
task-board handoff TASK-12 --role developer

# Task/Bug estimate enum
task-board m 'set_estimate(TASK-12, estimate=notEstimated)'
task-board m 'set_estimate(TASK-12, estimate=estimated, scale=fibonacci, number=5)'

# Create elements
task-board m 'create(type=task, parent=STORY-05, name=new-task)'
task-board m 'create(type=task, parent=STORY-05, name=new-task, description="Implement feature", scope="Backend only", ac="Tests pass")'

# Update README fields
task-board m 'set_details(TASK-12, title="New title", description="Updated desc")'

# Rename overview name/slug while preserving ID, README, progress, children, and resources
task-board m 'rename(TASK-12, name=better-task-name)'

# Assignment
task-board m 'assign(TASK-12, agent=claude-opus)'
task-board m 'unassign(TASK-12)'

# Dependencies
task-board m 'link(TASK-13, blocked_by=TASK-12)'
task-board m 'unlink(TASK-13, blocked_by=TASK-12)'

# Checklist
task-board m 'check_item(TASK-12, item=1)'
task-board m 'uncheck_item(TASK-12, item=1)'
task-board m 'add_checklist_item(TASK-12, text="Write tests")'

# Notes
task-board m 'set_notes(TASK-12, text="Progress update")'        # append
task-board m 'set_notes(TASK-12, text="Replace all", set=true)'  # replace

# Move to new parent
task-board m 'set_parent(TASK-12, parent=STORY-03)'

# Resources
task-board m 'add_resource(TASK-12, name="TASK-12_design.md", type=precondition, content="...", description="Design doc")'
task-board m 'update_resource(TASK-12, name="TASK-12_design.md", type=precondition, content="revised", description="Design doc v2")'
task-board m --confirm 'delete_resource(TASK-12, name="TASK-12_design.md")'

# Destructive — preview first, then confirm
task-board m --dry-run 'remove_checklist_item(TASK-12, item=2)'
task-board m --confirm 'remove_checklist_item(TASK-12, item=2)'
task-board m --dry-run 'delete(TASK-12)'
task-board m --confirm 'delete(TASK-12)'

# Batch mutations (semicolon-separated)
task-board m 'set_status(TASK-12, status=done); set_status(TASK-13, status=development)'
```

**Safety flags:**
- `--dry-run` — preview mutation without applying. Returns what would change
- `--confirm` — required for destructive mutations (`delete`, `delete_resource`)
- Mutation arguments are schema-checked. Unknown named arguments and extra positional fragments are rejected with typed errors before any write runs.
- Without `--confirm`, destructive mutations are rejected with an error

**Format:** `--format json` (default) or `--format compact`

**Introspection:** Run `task-board q 'schema(mutation=NAME)'` for one mutation signature, or full `schema()` for the entire contract.

`task-board handoff` resolves the role's configured end status, verifies all
checklist items plus an actual stored task-scoped outcome, and serializes that
decision with the ordinary status transition. The underlying shared `handoff`
mutation carries the already-resolved role/status for remote execution; use the
CLI facade so callers do not have to reproduce role-policy resolution.

The `estimate` q field is a nested enum. Task/Bug values are either
`{"kind":"notEstimated"}` or
`{"kind":"estimated","scale":{"kind":"fibonacci","number":5}}`.
Story/Epic values are calculated, never persisted or hand-set. Full coverage
returns the same `estimated` shape with the descendant sum. Partial coverage
returns
`{"kind":"partiallyEstimated","rolledEstimate":{"kind":"estimated","scale":{"kind":"fibonacci","number":5}},"notEstimatedCount":"1/3","unestimatedTaskIds":["TASK-..."]}`.
Story counts direct Task/Bug children; Epic rolls up descendant Task/Bug values.
New Task/Bug records must be estimated before entering `development`; legacy
records with no persisted Estimate section remain compatible and count as
unestimated in container projections.

## Grep — unstructured text search

When DSL can't help — searching for arbitrary text across board files.

```bash
# Find mentions of a term
task-board grep "AudioRecorder"

# Search in specific file type
task-board grep "blocked.*deployment" --file progress.md

# Case-insensitive search in descriptions
task-board grep "TODO" -i --file README.md

# With context lines
task-board grep "error" -C 2
```

## CLI — human-facing output and complex operations

CLI text output with ANSI colors is designed for humans. Use for operations not covered by DSL, or when human-readable output is needed.

When reporting board state to a person, reference each epic, story, task, or bug with both its concrete ID and its human-readable title/name. In practice, that usually means carrying both `id` and `name` in your query output, or using a preset such as `default` or `overview` that already includes them. ID-only references are not enough for chat summaries because readers should not have to resolve identifiers manually.

```bash
# Good human-facing summary inputs
task-board q --format compact 'list(type=task, status=development) { id name status }'
task-board q --format compact 'list(type=story, parent=EPIC-260218-abc123) { overview }'
```

### Parent alignment in the unified goal ledger

Use the top-level `goal` group to maintain one provider-neutral, board-scoped
statement of the current `parent/primary` owner's complete objective. It
survives a parent-session replacement, records immutable `board-goal-v2`
revisions, and never changes a board element's status or acceptance evidence.
The command group is the one-release `primary-goal-v1` compatibility adapter.

```bash
# Create only when no primary goal is active.
task-board goal set-primary \
  --objective "Keep the parent goal aligned with the current user requirements." \
  --reason "First actionable requirement"

# CAS-replace the whole objective. Repeating the same objective is idempotent.
task-board goal update --if-revision 1 \
  --objective "Keep the parent goal aligned with the current user requirements and active delivery work." \
  --reason "Requirement expanded"

# Read current state, immutable audit history, or one immutable revision.
task-board goal get
task-board goal get --history
task-board goal get --goal-id PRIMARY-GOAL-260723-a1b2c3 --revision 1

# Clear only with the observed revision and an explicit disposition reason.
task-board goal clear --if-revision 2 --reason "Parent intent superseded"
```

`goal get` is read-only and remains available inside a spawned run. When
`TASK_BOARD_RUN_ID` is set, `set-primary`, `update`, and `clear` return the
typed `primary_goal_parent_context_required` failure with no side effects;
spawned owners must use `task-board spawn goal` or `task-board spawn goal
upsert` for their `run/RUN-*` owner. Both commands use `board-goal-v2`; owner
isolation prevents either compatibility adapter from silently updating or
clearing the other.

Every successful JSON response has `ok`, `board_scope`, `primary_goal`, and
`latest_ref`; mutations additionally carry `mutation`. Exact revision reads
add `primary_goal_revision`, and history reads add newest-first `history`
summaries. Typed failures use
`{ok:false,error:{code,message,current_ref,side_effects:false,remediation?}}`.
Remote primary-adapter mutations accept authoritative `goal=board-v2` or the
one-release `primary_goal=board-v1` compatibility capability. Direct v2
mutations require `goal=board-v2`, and reads never create a local shadow
ledger.

## Decision table

| Need | Tool | Example |
|------|------|---------|
| Discover full API contract | DSL | `task-board q 'schema()'` |
| Repair one operation/mutation call | DSL | `task-board q 'schema(operation=NAME)'` / `schema(mutation=NAME)` |
| Preflight one spawn target | DSL | `task-board q 'project_config(view=spawn-preflight, role=ROLE, agent=PROVIDER)'` |
| Structured query (status, filter, lookup) | DSL query | `task-board q --format compact 'list(type=task, status=development) { overview }'` |
| Structured write (status, assign, link, create) | DSL mutation | `task-board m 'set_status(TASK-XX, status=development)'` |
| Evidence-backed producer handoff | CLI facade over shared mutation | `task-board handoff TASK-XX --role developer` |
| Board parent intent | CLI primary-goal group | `task-board goal get` / `task-board goal update --if-revision N ...` |
| Preview write without applying | DSL mutation | `task-board m --dry-run 'delete(TASK-XX)'` |
| Text search across elements | Grep | `task-board grep "AudioRecorder"` |
| **Phase planning (canonical API)** | **DSL query** | **`task-board q 'plan(EPIC-XX, mode=children)'`** |
| **Pretty-print / render / save a plan** | **CLI facade** | **`task-board plan EPIC-XX --render --format png`** |
| Resource management | DSL mutation / CLI | `task-board m 'add_resource(TASK-XX, name=TASK-XX_results.md, content="...", type=outcome)'` |

**IMPORTANT:** Canonical planning lives in `task-board q 'plan(...)'`. Use `task-board plan` only as a local pretty-print / render / save facade over that same planning model. Never manually create phase breakdowns or write `.planning/` files from scratch.

exec
/bin/zsh -lc "mkdir -p /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/TASK-260909-3kd1i7-integration
task-board --version > /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/TASK-260909-3kd1i7-integration/readiness-01.log
task-board q --format compact 'get(TASK-260909-3kd1i7) { status outcomeResources }; list(parent=STORY-260909-3g9457) { id name status }'" in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 21ms:
status:integrating
outcomeResources:[{"name":"TASK-260909-3kd1i7_spawn-log_-analyst--researcher--codex-_RUN-260908-f2d940.log","uri":"file://TASK-260909-3kd1i7/TASK-260909-3kd1i7_spawn-log_-analyst--researcher--codex-_RUN-260908-f2d940.log","description":"System spawn log captured by task-board"},{"name":"TASK-260909-3kd1i7_learning-exercises.md","uri":"file://TASK-260909-3kd1i7/TASK-260909-3kd1i7_learning-exercises.md","description":"Four source-grounded optional exercises, implemented/proposed boundaries, documentation corrections, and evidence logbook"},{"name":"TASK-260909-3kd1i7_change-request_rev1.patch","uri":"file://TASK-260909-3kd1i7/TASK-260909-3kd1i7_change-request_rev1.patch","description":"Change Request CR-TASK-260909-3kd1i7-1 revision 1 candidate patch (repository_delta=empty, 0 changed paths)"},{"name":"TASK-260909-3kd1i7_spawn-log_-reviewer--reviewer--codex-_RUN-260908-cf0c02.log","uri":"file://TASK-260909-3kd1i7/TASK-260909-3kd1i7_spawn-log_-reviewer--reviewer--codex-_RUN-260908-cf0c02.log","description":"System spawn log captured by task-board"},{"name":"TASK-260909-3kd1i7_review-verdict-rev1.md","uri":"file://TASK-260909-3kd1i7/TASK-260909-3kd1i7_review-verdict-rev1.md","description":"Accepted outline; source checks, AC coverage, empty-delta rationale and review bounds"},{"name":"TASK-260909-3kd1i7_spawn-log_-analyst--researcher--codex-_RUN-260908-809fa4.log","uri":"file://TASK-260909-3kd1i7/TASK-260909-3kd1i7_spawn-log_-analyst--researcher--codex-_RUN-260908-809fa4.log","description":"System spawn log captured by task-board"}]

id,name,status
TASK-260909-2p9bt6,document-architecture-diagrams,backlog
TASK-260909-3kd1i7,write-learning-exercises,integrating

exec
/bin/zsh -lc 'task-board worktree --help' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 0ms:
Inspect and repair the Story-scoped Git workspaces that repository-mutating
spawns run in.

Each Story gets one managed worktree under .temp/<STORY-ID>/worktree on a
short-lived task-board/story/<STORY-ID> branch forked from trunk. Trunk is
spawn.worktree_isolation.integration_base_branch when configured, and otherwise
resolves from refs/remotes/origin/HEAD and then the current branch.

These commands require a local authoritative board. On a remote board they
refuse with worktree_isolation_requires_local_board and do nothing else.

Usage:
  task-board worktree [command]

Available Commands:
  abort                 Stop using a Story workspace, preserving its work by default
  checkpoint            Commit an accepted leaf onto its Story branch while preserving integrating
  complete              Prove an accepted Story's code landed on its own repository, then record its board state in the board repository
  gc                    Release Story leases whose holding runs are provably terminal
  integrate             Land an accepted Story candidate on trunk and commit its board state
  invalidate-acceptance Release an element whose acceptance §6.2 already invalidated back to rework
  repair                Re-materialize a recorded workspace whose worktree is missing
  status                Report managed Story workspaces and what blocks their cleanup
  transaction           Inspect and dispose of a refused or abandoned integration transaction

Flags:
  -h, --help   help for worktree

Global Flags:
      --board-dir string         Path to the board directory (overrides TASK_BOARD_DIR and config local.board_dir) (default ".task-board")
      --context-profile string   Agent context profile override: full or lite
      --insecure                 Skip TLS certificate verification (for self-signed certs)
      --json                     Output in JSON format
      --no-update-check          Disable automatic update check
      --remote string            Remote board server URL (overrides TASK_BOARD_REMOTE)
      --remote-board string      Remote board ID (overrides TASK_BOARD_ID)

Use "task-board worktree [command] --help" for more information about a command.

exec
/bin/zsh -lc 'task-board worktree checkpoint TASK-260909-3kd1i7' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 618ms:
TASK-260909-3kd1i7: Change Request TASK-260909-3kd1i7 revision 1 has repository_delta=empty, so its checkpoint commit would have the same tree as its parent
TASK-260909-3kd1i7: status integrating

exec
/bin/zsh -lc 'task-board resource --help' in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 0ms:
Manage attached resources such as screenshots, docs, logs, and other files.

Use "task-board resource add" to create a new attached resource, "task-board
resource adopt" to register an orphaned local payload, "task-board resource
update" to replace an existing one, "task-board resource delete" to remove it,
and "task-board resource get" to materialize a resource back to the local
filesystem for inspection.

Usage:
  task-board resource [command]

Available Commands:
  add         Attach a new file resource to an element
  adopt       Register an existing unreferenced local resource payload
  delete      Delete a resource from an element (requires --confirm)
  get         Download a resource to the local filesystem
  update      Replace an existing file resource on an element

Flags:
  -h, --help   help for resource

Global Flags:
      --board-dir string         Path to the board directory (overrides TASK_BOARD_DIR and config local.board_dir) (default ".task-board")
      --context-profile string   Agent context profile override: full or lite
      --insecure                 Skip TLS certificate verification (for self-signed certs)
      --json                     Output in JSON format
      --no-update-check          Disable automatic update check
      --remote string            Remote board server URL (overrides TASK_BOARD_REMOTE)
      --remote-board string      Remote board ID (overrides TASK_BOARD_ID)

Use "task-board resource [command] --help" for more information about a command.

exec
/bin/zsh -lc "cat > /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/TASK-260909-3kd1i7-integration/TASK-260909-3kd1i7_integration-evidence.md <<'EOF'
# Learning exercises: accepted revision checkpoint

- Task: TASK-260909-3kd1i7 (write-learning-exercises).
- Accepted binding: CR-TASK-260909-3kd1i7-1, revision 1, researcher/analyst integration run.
- Existing deliverable: TASK-260909-3kd1i7_learning-exercises.md.
- Existing acceptance evidence: TASK-260909-3kd1i7_review-verdict-rev1.md.
- Fresh task-specific board read returned both resources and the empty repository-delta patch; exit code 0.
- The sibling document-architecture-diagrams task remains backlog, so this is a non-final leaf checkpoint.

## Command and observed result

Executed directly, without a pipe:

"'```text
task-board worktree checkpoint TASK-260909-3kd1i7
```

Exit code: **0**.

```text
TASK-260909-3kd1i7: Change Request TASK-260909-3kd1i7 revision 1 has repository_delta=empty, so its checkpoint commit would have the same tree as its parent
TASK-260909-3kd1i7: status integrating
```

No repository commit was created for the empty research delta. Status remains integrating pending Story integration. No generic producer handoff or manual done transition was used.

## Verification bounds

This run executed the bound checkpoint and checked the existing outcome inventory. It accepted the already-attached reviewer evidence; it did not rerun source research, application tests, or acceptance checks. No code, documentation, or configuration was edited in either managed worktree. No claim is made that application tests ran or passed in this run. The later documentation task remains responsible for publishing against final UI source paths.
EOF
task-board resource add TASK-260909-3kd1i7 /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/TASK-260909-3kd1i7-integration/TASK-260909-3kd1i7_integration-evidence.md --type outcome --name TASK-260909-3kd1i7_integration-evidence.md -d '"'Fresh accepted-revision checkpoint evidence: exit 0, empty repository delta, integrating pending Story landing'" in /Users/iv/Developer/IV/ioscrew-demo/relux-sample/.temp/STORY-260909-3g9457/worktree
 succeeded in 171ms:
Attached TASK-260909-3kd1i7_integration-evidence.md on TASK-260909-3kd1i7 as outcome

codex
TASK-260909-3kd1i7 (write-learning-exercises): checkpoint succeeded with exit code 0. Empty repository delta; no commit created.

Fresh integration evidence attached. Status remains `integrating`, pending Story landing.
tokens used
41,875
TASK-260909-3kd1i7 (write-learning-exercises): checkpoint succeeded with exit code 0. Empty repository delta; no commit created.

Fresh integration evidence attached. Status remains `integrating`, pending Story landing.
