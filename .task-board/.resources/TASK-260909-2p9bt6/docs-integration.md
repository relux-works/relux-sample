Integrate accepted Story locally, signed with configured human identity. Parent owns PR and hosted landing; do not push origin/main. TASK_BOARD_CONFIG points to task-scoped docs config whose validation renders PlantUML only. No app build, UI/snapshot tests or new product edits. If board activity moves during pre-movement window retry once with accepted source unchanged. Prior review passed 3/3 diagrams, 108/108 links and exact 20-file candidate match. Report final source/board commit IDs and signatures in concise outcome.