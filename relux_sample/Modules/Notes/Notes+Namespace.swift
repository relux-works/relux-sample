// module structure
enum Notes {
    enum Data {
        enum Api {
            enum DTO {}
        }
    }

    enum Business {
        enum Model {}
    }

    enum UI {
        enum Model {}
        enum Component {}

        enum List {}
        enum Details {}
        enum Create {}
        enum Edit {}
    }
}
